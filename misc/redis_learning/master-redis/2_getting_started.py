from pprint import pprint as pp

from utils import *

# print(ping())
# pp(info())
# pp(info('server'))
# pp(info('clients'))
pp(info('memory'))
# pp(info('stats'))
# pp(info('replication'))
# pp(info('cpu'))
# pp(info('cluster'))
# pp(info('keyspace'))
# pp(info('modules'))
